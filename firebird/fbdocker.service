[Unit]
Description=Firebird Container
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a fbserver
ExecStop=/usr/bin/docker stop -t 2 fbserver

[Install]
WantedBy=local.target
